import { Meta, Controls } from '@storybook/blocks'

import * as TableStories from './Table.stories'

<Meta of={TableStories} />

# Definition

Tables are a fundamental data representation format used to organize information into rows and columns, making it easier to understand, analyze, and present data in a structured manner.

## Usage

The component comes in three variants: `archived`, `active` and `summary` which you can use to alter the look and feel of the table.

## Inputs

Table has the following properties:

<Controls />
