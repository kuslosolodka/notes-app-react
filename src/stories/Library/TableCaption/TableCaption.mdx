import { Meta, Controls } from '@storybook/blocks'

import * as TableCaptionStories from './TableCaption.stories'

<Meta of={TableCaptionStories} />

# Definition

The Table Caption Component is a vital part of your application's user interface, specifically designed to provide a descriptive and meaningful caption for tables.

## Usage

Table caption using in table for displaying table name: `active notes`, `archived notes` and `summary`.

## Inputs

TableCaption has the following properties:

<Controls />
