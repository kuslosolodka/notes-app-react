import { Meta, Controls } from '@storybook/blocks'

import * as ErrorMessageStories from './ErrorMessage.stories'

<Meta of={ErrorMessageStories} />

# Definition

The Error Message Component is a vital part of application's user interface, designed to display informative error messages to users in cases of unexpected or erroneous behavior.

## Usage

Error message displaying error when validation is failing in input.

## Inputs

ErrorMessage has the following properties:

<Controls />
