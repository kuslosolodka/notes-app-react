import { Meta, Controls } from '@storybook/blocks'

import * as InputStories from './Input.stories'

<Meta of={InputStories} />

# Definition

User input refers to the data provided by end-users through interfaces like web forms.

Examples of user input include text entries, numeric values, dates, file uploads, and more.

## Usage

The component comes in three variants: `content`, `name` and `date` which you can use to alter the look and feel of the input.

## Inputs

Input has the following properties:

<Controls />
