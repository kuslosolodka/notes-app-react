import { Meta, Controls } from '@storybook/blocks'

import * as FormStories from './Form.stories'

<Meta of={FormStories} />

# Definition

The form collect specific information from users.

It serves as a tool for gathering data, opinions, or feedback efficiently.

## Usage

The component comes in two variants: `edit` and `add` which you can use to alter the look and feel of the form.

## Inputs

Form has the following properties:

<Controls />
