import { Meta, Controls } from '@storybook/blocks'

import * as TableRowStories from './TableRow.stories'

<Meta of={TableRowStories} />

# Definition

The Table Row Component is a core element of your application's table interface, designed to represent and display individual rows of data within a table.

## Usage

Table row using in table for displaying data in row
