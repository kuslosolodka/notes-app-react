import { Meta, Controls } from '@storybook/blocks'

import * as NoteListStories from './NoteList.stories'

<Meta of={NoteListStories} />

# Definition

The NoteList component is responsible for displaying a list of notes with various functionalities such as editing, deleting, and archiving notes.

It utilizes the Note component to represent each individual note within the list.

## Usage

NoteList displaying notes data

## Inputs

NoteList has the following properties:

<Controls />
