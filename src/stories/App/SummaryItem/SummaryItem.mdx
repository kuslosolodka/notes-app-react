import { Meta, Controls } from '@storybook/blocks'

import * as SummaryItemStories from './SummaryItem.stories'

<Meta of={SummaryItemStories} />

# Definition

The Summary Item Component is an integral part of the Summary List Component, designed to display summarized information about different categories from both archived and active notes tables.

## Usage

Summary Item displaying summary details: `category`, `counts of categories in active table`, `counts of categories in archived table`.

## Inputs

SummaryItem has the following properties:

<Controls />
